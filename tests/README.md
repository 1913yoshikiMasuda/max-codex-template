# Tests

Run `npm test`. The suite uses small JSON fixtures and does not launch Max. It covers valid parsing, recursive subpatcher detection, missing connection endpoints, overlap warnings, and duplicate semantic names.
