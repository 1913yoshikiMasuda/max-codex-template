# Test

`npm run max:test` を実行してください。このtest suiteは小さなJSON fixtureを使用し、Maxを起動しません。妥当なparse、recursive subpatcher検出、connection endpointの欠落、overlap warning、semantic nameの重複、help sourceの探索、project patch-globの解決、read-onlyの `.maxproj` 照合、project bootstrapのpreview/write/re-run防止を対象とします。
